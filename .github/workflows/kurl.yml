name: kurl CI

on:
  push:
    branches: [main]
    paths:
      - "api/**"
      - "app/**"
      - ".github/workflows/kurl.yml"
  pull_request:
    branches: [main]
    paths:
      - "api/**"
      - "app/**"
      - ".github/workflows/kurl.yml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  changes:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    outputs:
      api: ${{ steps.filter.outputs.api }}
      app: ${{ steps.filter.outputs.app }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            api:
              - 'api/**'
            app:
              - 'app/**'

  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    defaults:
      run:
        working-directory: api

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Check for syntax errors
        run: find . -name "*.py" -not -path "./.venv/*" -not -path "./__pycache__/*" -not -path "./python_modules/*" | xargs python -m py_compile

  test:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: [lint]
    defaults:
      run:
        working-directory: api

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install test dependencies
        run: pip install pytest pytest-asyncio httpx PyJWT

      - name: Run tests
        run: pytest -q

  deploy-api:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: [changes, lint, test]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main' && needs.changes.outputs.api == 'true'

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install pywrangler
        run: |
          pip install uv
          uv tool install workers-py

      - name: Deploy to Cloudflare Workers
        working-directory: api
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: pywrangler deploy

  smoke-test:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: [deploy-api]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main' && needs.changes.outputs.api == 'true'

    steps:
      - name: Test endpoints
        run: |
          set -e
          WORKER_URL="${{ secrets.KURL_WORKER_URL }}"
          AUTH="X-API-Key: ${{ secrets.KURL_API_KEY }}"

          echo "Testing GET /api (public)..."
          curl -sS "$WORKER_URL/api" | python3 -c "import sys,json; d=json.load(sys.stdin); assert d['status']=='OK', f'Unexpected: {d}'; print('API info passed')"

          echo "Testing GET /api/healthz (public)..."
          curl -sS "$WORKER_URL/api/healthz" | python3 -c "import sys,json; d=json.load(sys.stdin); assert d['status']=='healthy', f'Unexpected: {d}'; print('Health check passed')"

          echo "Testing GET /api/readyz (auth)..."
          curl -sS -H "$AUTH" "$WORKER_URL/api/readyz" | python3 -c "import sys,json; d=json.load(sys.stdin); assert d['status'] in ('ready','degraded'), f'Unexpected: {d}'; print('Readiness check passed')"

          echo "Testing POST /api/kurl (public)..."
          curl -sS -X POST -H "Content-Type: application/json" -d '{"url":"https://open.spotify.com/track/4cOdK2wGLETKBW3PvgPWqT","target_platform":"deezer"}' "$WORKER_URL/api/kurl" | python3 -c "import sys,json; d=json.load(sys.stdin); assert d['status']=='success', f'Unexpected: {d}'; print('Kurl endpoint passed')"

          echo "Testing auth enforcement (summary without key should 401)..."
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" "$WORKER_URL/api/events/summary")
          [ "$STATUS" = "401" ] && echo "Auth enforcement passed" || (echo "Expected 401, got $STATUS" && exit 1)

          echo "All smoke tests passed"

  deploy-web:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: [changes]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main' && needs.changes.outputs.app == 'true'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          # Pin until simple_icons publishes a Flutter 3.44+ compatible release.
          # 3.44 made IconData final, breaking simple_icons 14.6.1.
          flutter-version: "3.41.9"

      - name: Build Flutter web
        working-directory: app
        run: flutter build web --release --dart-define=KURL_API_KEY=${{ secrets.KURL_API_KEY }}

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_PAGES_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          wranglerVersion: "4"
          command: pages deploy app/build/web --project-name kurl-web
