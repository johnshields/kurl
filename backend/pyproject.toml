[project]
name = "kurl-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "httpx",
    "pydantic",
    "PyJWT",
]

[tool.pytest.ini_options]
testpaths = ["__tests__"]
asyncio_mode = "auto"
