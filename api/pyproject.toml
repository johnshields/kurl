[project]
name = "kurl-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "httpx",
    "PyJWT",
]

[tool.pytest.ini_options]
testpaths = ["src/__tests__"]
pythonpath = ["src"]
asyncio_mode = "auto"
